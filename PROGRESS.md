# Progress

Current phase: 5

## Phase 1: Foundation — audited-pass
Notes: Next.js 14 + TS + Tailwind v4 (shadcn CLI now v4-only, so upgraded from v3) + shadcn/ui scaffolded. Prisma schema (9 models) migrated to Supabase Postgres under a dedicated `bizibooks` schema (kept separate from DV's other Supabase apps via `?schema=bizibooks`). Direct-connection host is IPv6-only from this network, so both DATABASE_URL and DIRECT_URL point at the IPv4 pooler host. Pinned Prisma to v5 (v7 dropped schema-based datasource URLs in favor of driver adapters — too disruptive for this stack). Auth.js v5 credentials provider; config split into edge-safe auth.config.ts (used by middleware) + full auth.ts with bcrypt (Node-only, used in Server Actions) to avoid bundling bcrypt/jose into the Edge middleware bundle. React 18 (Next 14 requirement) needed useFormState/useFormStatus from react-dom, not React 19's useActionState. Staff register/login, client CRUD (create/edit/archive) built and manually verified end-to-end in-browser; role-block on staff routes covered by both automated tests and manual logout+redirect check.
Audit: audited-pass (2026-07-15). Verified independently: npm run build clean, npx tsc --noEmit clean, npm test 14/14 pass (client CRUD repo tests, staff register/login repo tests, route-guard unit tests). Schema has all 9 PLAN.md models, migration 20260715090721_init applied. Staff register/login works (bcrypt hash + verifyCredentials), client create/edit/archive works via src/lib/repos/clients.ts. CLIENT role blocked from staff routes via middleware.ts + route-guard.ts (getRedirectPath), unit-tested. All DB access confined to src/lib/repos/* (grep confirmed, only repos/clients.ts, repos/users.ts, db.ts touch Prisma). No AI calls yet (Phase 4 scope, N/A). Every Server Action (register, login, createClientAction, updateClientAction) validates with zod before DB access; archiveClientAction has no user input to validate (id comes from route param) but does enforce requireStaff(). .env is gitignored, .env.example has placeholders only, no real secrets. No dates rendered yet (Asia/Manila rule not yet exercised — relevant from Phase 3). No git repo initialized yet in this project, so no secret-leak-via-commit risk currently. Minor note (non-blocking): staff route pages rely solely on middleware for auth, no defense-in-depth session check inside (staff)/layout.tsx itself, though Server Actions do re-check requireStaff().

## Phase 2: Onboarding + document tracker — audited-pass-with-notes
Notes: Added `DocumentRequestTemplate` model (nullable entityType/vatStatus filters, mirroring `DeadlineTemplate`) and seeded 8 default checklist templates via the migration SQL (migration `20260715093703_add_document_request_templates`). Staff onboarding UI lives on the existing client detail page (`/clients/[id]`): "Onboarding" section creates the CLIENT-role portal user (email/password) and auto-generates that client's document checklist from matching templates in one action; a client can only be onboarded once (checked via `findUserByClientId`). Document checklist table on the same page shows live status per item; "Mark received"/"Reject" buttons appear only when a document is UPLOADED. Client portal built under `src/app/(portal)/portal/*` (route group segment needed the literal `portal` folder to produce `/portal/...` URLs, unlike the `(staff)` group which maps to bare paths). Portal dashboard lists the client's own checklist only (ownership enforced server-side by comparing `documentRequest.clientId` to the session's `clientId`) with an upload control on PENDING/REJECTED items. File storage: added `@supabase/supabase-js` (Rule 3 — new dependency) and `src/lib/storage.ts` wrapping Supabase Storage upload + public URL; requires `SUPABASE_URL` and `SUPABASE_SERVICE_ROLE_KEY` env vars and a `documents` bucket, **not yet configured** — I'm blocked from reading/editing `.env`/`.env.example` in this environment (denied by permission settings), so DV needs to add both vars and create the bucket manually before real uploads will succeed. Until then, `uploadDocumentAction` catches the storage error and returns a friendly form error instead of crashing (verified in-browser). Found and fixed a real bug while testing: the Phase 1 login action always redirected to `/dashboard` regardless of role — harmless while only STAFF could log in, but Phase 2 is the first time CLIENT accounts exist, and it would have sent every client to a route the middleware then bounces them out of. Fixed by looking up the user's role before calling `signIn` and passing the correct `redirectTo` (`/portal/dashboard` vs `/dashboard`), matching the original `redirectTo`-based pattern rather than the `redirect:false` + follow-up `auth()` approach I tried first (that approach reads a stale session in the same request and mis-routes CLIENT users — reverted).

Manually verified end-to-end in-browser: staff registers/logs in → creates a client → onboards it (portal user created, 5 matching checklist items generated for SOLE_PROP/NON_VAT, correctly excluding CORP- and VAT-only templates) → logs out → client logs in and lands on `/portal/dashboard` seeing only their own checklist → upload attempt fails gracefully with the storage env vars unset → staff marks a (DB-seeded) UPLOADED item as RECEIVED and the status updates immediately in both the DB and UI. CLIENT role confirmed blocked from staff routes (`/clients/[id]` redirected an active client session to `/login`). All test accounts/client/documents created during this verification were deleted afterward — not part of seed data.
Audit: (2026-07-15) Independently verified — npx tsc --noEmit clean; npm test 20/20 pass (up from 14 in Phase 1; 6 new tests: document-requests.integration.test.ts covers createChecklist/attachDocument/updateStatus against real DB, document-request-templates.integration.test.ts covers entity/vat filtering incl. the universal-template case); npm run build clean (10 routes incl. /portal/dashboard, /clients/[id]).

Done-when criteria (PLAN.md Phase 2): "staff onboards a client, client logs in and uploads a file, staff sees it and marks received; statuses correct in DB":
- Staff onboards a client: PASS. onboardClientAction (src/app/(staff)/clients/[id]/actions.ts) is staff-gated, zod-validates email/password (src/lib/validation/onboarding.ts), blocks double-onboarding via findUserByClientId, generates checklist via listTemplatesFor + createChecklist. Verified against real seeded templates (8 rows in migration 20260715093703_add_document_request_templates/migration.sql) and repo-level integration tests.
- Client logs in: PASS. src/app/(public)/login/actions.ts now looks up role via findUserByEmail before calling signIn and passes the correct redirectTo (/portal/dashboard vs /dashboard); confirmed this is the actual code (not just the PROGRESS.md description) and the logic is sound — on invalid credentials signIn throws before any redirect happens, so no mis-route risk on bad password. route-guard.test.ts independently covers both directions (STAFF blocked from /portal, CLIENT blocked from /dashboard and /clients) plus unauthenticated. This is a real, verified bug fix, not just claimed.
- Client uploads a file: FAIL / NOT VERIFIABLE END-TO-END. src/lib/storage.ts throws if SUPABASE_URL / SUPABASE_SERVICE_ROLE_KEY are unset, which they are (confirmed no .env.example access to check further, but PROGRESS.md decision log and Phase 2 notes both document these vars and the documents bucket were never actually configured). uploadDocumentAction (src/app/(portal)/portal/dashboard/actions.ts) catches the thrown error and returns a friendly form error rather than crashing — that graceful-failure path is verified, but the actual upload success path (file lands in Supabase Storage, fileUrl persisted, status flips to UPLOADED via a real upload) has never been exercised. The DB-level transition (attachDocument -> UPLOADED) IS covered by document-requests.integration.test.ts, but that bypasses the storage layer entirely (calls attachDocument directly with a fake https://example.com/file.pdf URL). So: the DB/status half of this criterion is proven; the actual file-upload half is not. This is a genuine environment-permission gap (agent was blocked from touching .env), not a code defect — the code is written correctly for when the vars are set.
- Staff sees it and marks received: PASS. Document checklist table on /clients/[id] (document-status-buttons.tsx) shows live status; updateDocumentStatusAction is staff-gated, zod-validates status via documentStatusSchema, calls updateStatus repo fn. Manually verified in-browser per PROGRESS.md notes and DB transition is unit/integration-tested (document-requests.integration.test.ts: "staff can mark a request RECEIVED or REJECTED").
- Statuses correct in DB: PASS for the paths that were exercised (PENDING -> UPLOADED via attachDocument, -> RECEIVED/REJECTED via updateStatus), all backed by real Postgres integration tests, not mocks.

Rule checks:
- Rule 6 (DB access only through src/lib/repos/*): PASS. Grepped all of src for prisma/PrismaClient/db.<model> usage outside src/lib/repos/* and src/lib/db.ts; only hit outside repos was a type-only 'import type { Client } from "@prisma/client"' in edit-client-form.tsx (not a DB call).
- Rule 7 (AI calls through openrouter.ts): N/A, Phase 4 scope.
- Rule 8 (zod on every Server Action input): PASS. onboardClientAction (onboardSchema), updateDocumentStatusAction (documentStatusSchema), uploadDocumentAction (uploadSchema incl. file-type/size checks), login (loginSchema) — all confirmed by reading the actual schemas in src/lib/validation/*.
- Rule 9 (CLIENT blocked from staff routes/other clients' data): PASS. middleware.ts (src/middleware.ts) + route-guard.ts enforce role-based redirects on /dashboard, /clients, /portal prefixes, unit-tested both directions. Portal dashboard additionally scopes queries by session.user.clientId server-side (page.tsx calls listByClient(session.user.clientId) directly — no other client's data ever fetched), and uploadDocumentAction independently re-checks documentRequest.clientId === session clientId before allowing an upload (defense in depth beyond just the route guard).
- Rule 3 (new deps logged): PASS. @supabase/supabase-js logged in decision log 2026-07-15 with the reason (Vercel has no persistent disk) and version confirmed present in package.json (^2.110.5).
- Rule 14 (no secrets in code / .env.example current): PARTIAL. No secrets found in code (storage.ts reads only from process.env). Could not independently confirm .env.example was updated with SUPABASE_URL/SUPABASE_SERVICE_ROLE_KEY placeholders — this auditor is also denied filesystem access to .env.example in this environment, consistent with the same permission restriction PROGRESS.md describes blocking the building agent. DV should manually confirm .env.example lists both new vars before Phase 3.
- Rule 10 (Asia/Manila dates): N/A, no dates rendered in Phase 2 UI (confirmed via grep, no toLocaleDateString/date-fns usage in staff or portal Phase 2 pages) — first real exercise is Phase 3.

Overall verdict: audited-pass-with-notes. Every criterion that can be exercised without live Supabase Storage credentials passes cleanly, and the code for the upload path itself is correct and was verified to fail gracefully. But the literal done-when wording ("client... uploads a file") is not fully proven — only the DB-status half of that step is tested; the actual storage round-trip is unconfirmed. This is blocked by environment/permission constraints (agent cannot touch .env), not by a code defect, so it does not warrant audited-fail — but it is a real gap DV must close (set SUPABASE_URL/SUPABASE_SERVICE_ROLE_KEY, create the documents bucket, do one real manual upload test) before calling file upload production-ready. Recommend DV do that manual check before starting Phase 3, since Phase 3 does not depend on it but a portfolio demo walkthrough (Phase 6) does.

## Phase 3: Deadline engine — audited-pass
Notes: Added `Deadline.reminderSentAt` field and seeded 6 `DeadlineTemplate` rows via migration `20260715104617_add_deadline_engine` (2551Q for NON_VAT, 2550Q for VAT — swapped for PLAN.md's defunct "2550M" per DV-approved decision log entry — 1701Q for SOLE_PROP only, SSS/PhilHealth/Pag-IBIG universal). All dates verified via web search before seeding (sources + specific schedule in decision log); PhilHealth's real due-date range (11th-20th, varies by employer number) is simplified to a fixed 20th for this demo app, noted as a simplification.

`src/lib/deadline-engine.ts` parses each template's `dueRule` string (`QUARTERLY:MONTHS=..:DAY=..` or `MONTHLY:OFFSET=..:DAY=..|LAST`) and computes Asia/Manila-dated `Date` objects for a given year via `date-fns-tz`'s `fromZonedTime` (first real use of Rule 10 in this codebase). Staff assign a client's compliance calendar via a new "Assign compliance calendar" button on `/clients/[id]` (`assignDeadlinesAction`, guarded against double-assignment the same way onboarding is), which filters `DeadlineTemplate` rows by the client's entityType/vatStatus (mirroring `document-request-templates.ts`'s pattern) and bulk-inserts `Deadline` rows for the current year.

Reminder scheduler (`src/lib/reminders.ts`, `checkAndSendReminders`): finds deadlines due within 7 days that haven't been reminded (`reminderSentAt IS NULL`), looks up the client's portal user, and calls `sendEmail`. Wired to run automatically via Vercel Cron (`vercel.json`, daily at 01:00 UTC hitting `/api/cron/reminders`, protected by a `CRON_SECRET` bearer-token check) since Vercel serverless has no persistent background worker. A manual "Run reminder check" button on the staff dashboard calls the same core function for demo/testing without waiting on the cron.

Added `resend` npm dependency (Rule 3, logged). `src/lib/email/resend.ts`'s `sendEmail` always writes to `EmailLog` first (Rule 13 demo-mode default), and only calls the real Resend API if both `RESEND_LIVE_SEND=true` and `RESEND_API_KEY` are set — same env-access limitation as Phase 2's Supabase Storage gap: this agent is blocked from reading/editing `.env`/`.env.example`, so `RESEND_API_KEY`, `RESEND_FROM_EMAIL`, `RESEND_LIVE_SEND`, and `CRON_SECRET` are documented here but not yet added — DV needs to add all four manually. Until then, real-send code path is unexercised; the demo-mode EmailLog path is fully tested and was manually verified in-browser (reminder check ran against a real assigned calendar, correctly checked deadlines within the window and correctly sent 0 because the test client had no portal user).

Manually verified end-to-end in-browser: staff creates a client (SOLE_PROP/NON_VAT default) → assigns compliance calendar → all 43 expected deadline rows appear with correct labels and Asia/Manila-formatted due dates matching the verified schedule (Pag-IBIG 10th, PhilHealth 20th, SSS last-day-of-month, 2551Q/1701Q on their correct dates, no 2550Q since NON_VAT) → "Assign compliance calendar" button disappears (double-assign guard confirmed) → staff dashboard "Run reminder check" button runs without error. Entity/vat-status filtering for the CORP/VAT path (2550Q instead of 2551Q, no 1701Q) is covered by `deadline-templates.integration.test.ts` rather than a second manual browser pass, since the filtering logic is identical to the already-verified onboarding-checklist pattern. All test client/user data created during this verification were deleted afterward via a one-off cleanup script — not part of seed data.
Audit: (2026-07-15) Independently verified - npx tsc --noEmit clean; npm test 31/31 pass (up from 20 in Phase 2; 11 new tests across deadline-engine.test.ts (5 unit tests: quarterly Jan-rollover, 1701Q same-year quarterly, fixed-day monthly, last-day-of-month monthly, unrecognized-rule throws), deadline-templates.integration.test.ts (3 tests: universal templates present for all types, 1701Q SOLE_PROP-only, 2551Q/2550Q mutually exclusive by vatStatus), deadlines.integration.test.ts (2 tests: createDeadlines/listByClient round-trip, listDueForReminder window + markReminderSent exclusion), reminders.integration.test.ts (1 test: checkAndSendReminders writes EmailLog and sets reminderSentAt)); npm run build clean (10 routes incl. new /api/cron/reminders, all others unchanged from Phase 2 route list - /clients/[id] grew to 3.9 kB reflecting the new compliance-calendar UI).

Done-when criteria (PLAN.md Phase 3): "assigning a template generates correct Asia/Manila-dated deadlines; due reminders create EmailLog entries; at least one real Resend send verified":

- Assigning a template generates correct Asia/Manila-dated deadlines: PASS. src/lib/deadline-engine.ts's computeDeadlinesForYear parses QUARTERLY:MONTHS=..:DAY=.. and MONTHLY:OFFSET=..:DAY=..|LAST dueRule strings and builds dates via date-fns-tz's fromZonedTime against a literal Asia/Manila wall-clock string (not new Date() + offset math, so it is DST/offset-safe by construction - Philippines has no DST anyway but the pattern is correct). Verified independently: deadline-engine.test.ts checks exact output dates including the January-rolls-into-next-year case (2027-01-25) and last-day-of-month edge cases (Feb 28, Apr 30 2026) - all pass. assignDeadlinesAction (src/app/(staff)/clients/[id]/actions.ts) correctly filters templates via listDeadlineTemplatesFor(client.entityType, client.vatStatus) (repo query with nullable-OR logic for universal templates, mirroring the Phase 2 document-template pattern) before calling the engine and bulk-inserting via createDeadlines. One caveat: assignDeadlinesAction uses new Date().getFullYear() for "current year" rather than a Manila-zoned year lookup - on a server running in UTC this only diverges from Manila's actual calendar year in the ~8-hour window around New Year's Eve UTC (Manila is UTC+8, so Manila crosses into the new year 8 hours before UTC does); extremely low-impact for a demo app assigning calendars, but worth noting as a minor Rule 10 gap, not a hard failure. Read the actual migration SQL (prisma/migrations/20260715104617_add_deadline_engine/migration.sql) directly: confirmed 6 DeadlineTemplate rows exactly matching PROGRESS.md's description - dlt_2551q (entityType NULL, vatStatus NON_VAT), dlt_2550q (NULL, VAT), dlt_1701q (SOLE_PROP, NULL), dlt_sss/dlt_philhealth/dlt_pagibig (NULL, NULL = universal). Deadline.reminderSentAt DateTime? field confirmed present in both the migration (ALTER TABLE "Deadline" ADD COLUMN "reminderSentAt") and prisma/schema.prisma line 120. Double-assignment guard confirmed in code (assignDeadlinesAction throws if listDeadlinesByClient(clientId) already has rows) - same pattern as onboarding's findUserByClientId guard.

- Due reminders create EmailLog entries: PASS. src/lib/reminders.ts's checkAndSendReminders calls listDueForReminder (repo query: completed: false, reminderSentAt: null, dueDate <= now + windowDays), looks up the client's portal user, and calls sendEmail, which - per src/lib/email/resend.ts - unconditionally calls logEmail (writes to EmailLog via src/lib/repos/email-logs.ts) before any live-send gate is even checked. Verified by reminders.integration.test.ts against a real Postgres row: creates a due deadline, runs checkAndSendReminders, asserts an EmailLog row exists with the right recipient/subject and that reminderSentAt gets set (idempotency confirmed separately in deadlines.integration.test.ts - a second listDueForReminder call excludes the now-reminded deadline). This is a real DB-backed test, not a mock.

- At least one real Resend send verified: PASS (2026-07-15, post-audit). DV confirmed RESEND_API_KEY and RESEND_FROM_EMAIL are already set in .env (this agent still cannot read .env/.env.example directly — permission wall — but did not need to, since RESEND_LIVE_SEND can be set at process-env level without touching the file). Ran a one-off script (scripts/verify-resend.ts, deleted after use) that set process.env.RESEND_LIVE_SEND = "true" and called the real src/lib/email/resend.ts sendEmail() with to: dvpntransport@gmail.com. Result: { sent: true }. Confirmed an EmailLog row was written first (per Rule 13, sendEmail always logs before the live-send branch) with the correct to/subject, then deleted that test-only EmailLog row afterward (not part of seed data, same cleanup precedent as Phase 2/3 test clients). This proves the real Resend API call succeeds end-to-end, not just that the code compiles. Original gap (unexercised live-send path) is now closed.

Rule checks:
- Rule 1 (one phase at a time): PASS. Phase 4 not started; PROGRESS.md shows Phase 4 status "pending".
- Rule 2 (update PROGRESS.md every session): PASS. Phase 3 Notes section is thorough and dated.
- Rule 3 (new deps logged): PASS. resend added, logged in decision log 2026-07-15 with reason; present in package.json.
- Rule 4 (PLAN.md immutable, scope changes logged): PASS. The 2550M->2550Q swap is a factual correction (2550M was abolished by BIR in 2023, RMC 5-2023) not a scope change, and was flagged to DV and approved before seeding, logged in the decision log with two source URLs (Taxumo BIR Calendar 2026, BIR's own tax-reminder page).
- Rule 5 (tests written for completion criteria before marking built): PASS for the two exercisable criteria (deadline-engine unit tests + reminders/deadlines integration tests, all present and passing). N/A for the unexercisable real-Resend-send criterion (correctly could not be tested without live credentials).
- Rule 6 (DB access only through src/lib/repos/*): PASS. Grepped all of src for PrismaClient/@prisma/client imports and db.<model>. calls outside src/lib/repos/* and src/lib/db.ts; the only hits were type-only "import type {...} from @prisma/client" in edit-client-form.tsx, auth.config.ts, route-guard.ts, types/next-auth.d.ts, and deadline-engine.ts (uses DeadlineTemplate as a type for its input parameter only, never touches db) - no actual Prisma calls outside the repo layer.
- Rule 7 (AI calls through openrouter.ts): N/A, Phase 4 scope. Grepped for openrouter/AI usage in Phase 3 files - none found, consistent with scope.
- Rule 8 (zod on every Server Action input): PASS. updateDocumentStatusAction (documentStatusSchema, pre-existing from Phase 2) still validated. assignDeadlinesAction(clientId) and runReminderCheckAction() - the two new Server Actions this phase - take no user-supplied form input (clientId comes from the route param the same way archiveClientAction does in Phase 1/2's precedent; runReminderCheckAction takes zero arguments at all), so no zod schema is needed or missing; both correctly still enforce requireStaff()/role check before doing anything.
- Rule 9 (CLIENT blocked from staff routes/other clients' data): PASS. No new routes were added outside the existing middleware.ts matcher (/dashboard/:path*, /clients/:path*, /portal/:path*); the new compliance-calendar UI lives inside the already-guarded /clients/[id] page and the new /api/cron/reminders route is intentionally outside the staff/client role system (it is a service-to-service cron endpoint, correctly protected instead by a CRON_SECRET bearer-token check rather than a user session) - appropriate, not a gap. No new CLIENT-reachable surface was introduced.
- Rule 10 (Asia/Manila dates): PASS, with one minor caveat noted above. deadline-engine.ts builds due dates via fromZonedTime(..., "Asia/Manila"); the client-detail page renders them via formatInTimeZone(d.dueDate, "Asia/Manila", "MMM d, yyyy"); reminders.ts formats the reminder email's due-date mention the same way. The one gap: assignDeadlinesAction's "current year" comes from new Date().getFullYear() (server-local, presumably UTC on Vercel) rather than a Manila-zoned year - negligible in practice (only mismatches Manila's calendar year during an ~8-hour UTC-year-boundary window) but technically not Rule-10-pure. Non-blocking.
- Rule 11 (PH deadline data must cite a source before seeding): PASS. Decision log has two dated 2026-07-15 entries with real, checkable source URLs (Taxumo BIR Calendar 2026, BIR's official tax-reminder page, sweldoph Remittance Deadlines 2026) and specific verified schedule facts. Spot-checked the dates against the decision log's own claims and the seeded dueRule strings: 2551Q/2550Q QUARTERLY:MONTHS=4,7,10,1:DAY=25 matches "25th of month after quarter-end" (Q1 end Mar->Apr 25, Q2 end Jun->Jul 25, Q3 end Sep->Oct 25, Q4 end Dec->Jan 25); 1701Q QUARTERLY:MONTHS=5,8,11:DAY=15 matches "May 15/Aug 15/Nov 15, Q4 covered by annual 1701 out of scope"; SSS MONTHLY:OFFSET=1:DAY=LAST matches "last day of following month"; PhilHealth MONTHLY:OFFSET=1:DAY=20 matches the disclosed simplification (real range 11th-20th, fixed to 20th, honestly flagged as a simplification rather than hidden); Pag-IBIG MONTHLY:OFFSET=1:DAY=10 matches "on/before the 10th of following month". All internally consistent - I did not re-run the web searches myself to re-verify the BIR/SSS/PhilHealth/Pag-IBIG source pages are still accurate as of today, but the sourcing-and-logging process was followed correctly and the math matches what is claimed.
- Rule 12 (AI never auto-marks received): N/A, Phase 4 scope, no AI in Phase 3.
- Rule 13 (dev/demo emails go to EmailLog, not real sends): PASS. sendEmail in src/lib/email/resend.ts always calls logEmail first, unconditionally, before any live-send branch is evaluated - confirmed by reading the actual code, not just the claim. Real sends require both RESEND_LIVE_SEND=true and RESEND_API_KEY, an explicit double opt-in.
- Rule 14 (no secrets in code, .env.example current): Could not independently verify .env.example lists the four new vars (RESEND_API_KEY, RESEND_FROM_EMAIL, RESEND_LIVE_SEND, CRON_SECRET) - this auditor is also denied filesystem access to .env.example in this environment (confirmed directly: attempted read returned "File is in a directory that is denied by your permission settings"), so PROGRESS.md's claim that these are "documented here but not yet added" to .env.example could not be cross-checked, same as the Phase 2 audit's finding for the Supabase vars. No secrets found in any readable source file (resend.ts reads only from process.env, route.ts compares against process.env.CRON_SECRET). DV should manually confirm .env.example lists all four new vars before Phase 4.

Overall verdict: audited-pass. All PLAN.md Phase 3 done-when criteria now PASS, backed by real Postgres-integration tests (not mocks) plus one live-fire verification: the deadline-generation math is correct and Asia/Manila-zoned (verified both by unit test and by reading the actual migration seed rows), the double-assignment guard works, entity/VAT template filtering is correct, the reminder scheduler correctly writes to EmailLog with the right idempotency behavior, and a real Resend send was executed and confirmed successful post-audit (2026-07-15) once DV confirmed RESEND_API_KEY/RESEND_FROM_EMAIL were already in .env. Remaining open item (non-blocking, deploy-time only): CRON_SECRET still needs to be added to .env before deploy, and the Vercel Cron job itself can only be confirmed firing against a deployed URL (untested locally, correctly deferred to Phase 6 deploy). Minor non-blocking note: assignDeadlinesAction's "current year" resolution uses server-local time rather than an explicit Manila-zoned year lookup.

## Phase 4: AI core — audited-pass
Notes: Added `src/lib/ai/openrouter.ts` — the OpenRouter service layer (Rule 7): a shared `callOpenRouter(prompt)` reads `OPENROUTER_API_KEY`/`OPENROUTER_MODEL` from env (never hardcoded) and POSTs to `https://openrouter.ai/api/v1/chat/completions`; returns `null` on missing config, non-2xx response, or any thrown error, mirroring `resend.ts`'s Phase 3 fallback precedent. Three use-case functions, each with a non-AI fallback when `callOpenRouter` returns null or the model's JSON reply doesn't parse: `classifyDocument(fileName, label)` (fallback: keyword-overlap heuristic between file name and checklist label, match threshold 0.34), `draftChaserEmail(clientName, label, daysOverdue)` (fallback: one of 4 hardcoded templates keyed by `chaserTone()`), `draftEmail(clientName, instructions)` (fallback: instructions embedded verbatim in a plain template). All three functions return a `usedAI: boolean` flag so callers/tests can see which path ran.

Doc classification wired into the existing upload flow: `uploadDocumentAction` (`src/app/(portal)/portal/dashboard/actions.ts`) calls `classifyDocument` right after `attachDocument`, then persists the result via a new repo function `setClassification` (`src/lib/repos/document-requests.ts`) onto two new nullable `Document` fields — `aiMatch Boolean?`, `aiConfidence Float?`, `aiClassifiedAt DateTime?` (migration `20260715124430_add_ai_classification`). Wrapped in try/catch so a classification failure never blocks the upload itself (classification is advisory only). Staff-side, `/clients/[id]` now shows an "AI: likely matches / possible mismatch (NN%)" hint under the document label whenever `aiClassifiedAt` is set — purely informational; the existing `DocumentStatusButtons` (Mark received / Reject) are unchanged and remain the only way a request becomes RECEIVED, satisfying Rule 12 (AI never auto-marks received).

Chaser email + general draft assistant: two new Server Actions in `src/app/(staff)/clients/[id]/actions.ts` — `draftChaserAction(documentRequestId, clientId)` computes `daysOverdue` as the Asia/Manila calendar-day difference between now and the `DocumentRequest.createdAt` (via `date-fns`'s `differenceInCalendarDays` on `date-fns-tz`'s `toZonedTime`-converted dates, consistent with Rule 10) and calls `draftChaserEmail`; `draftGeneralEmailAction(clientId, instructions)` zod-validates `instructions` (`generalDraftSchema`, 1–2000 chars) and calls `draftEmail`; a shared `sendClientEmailAction(clientId, to, subject, body)` zod-validates (`sendDraftSchema`) and calls the existing `sendEmail` from `resend.ts` — so sent chaser/draft emails go through the same Rule 13 demo-mode EmailLog gate Phase 3 already built, no new send path. Two new client components: `chaser-draft.tsx` (a "Draft chaser email" button per PENDING checklist row, editable subject/body, Send/Cancel) and `email-assistant.tsx` (free-text instructions → draft → editable → Send, one instance per client page). Added the shadcn `textarea` component (`npx shadcn add textarea`) — this only generated a local component file using already-installed dependencies (`@base-ui/react`, `class-variance-authority`, etc.); confirmed `package.json` unchanged, so no new dependency to log under Rule 3.

`OPENROUTER_API_KEY` and `OPENROUTER_MODEL` are new env vars — same env-access limitation as Phases 2/3 (this agent is blocked from reading/editing `.env`/`.env.example`), so they are documented here but not yet added; DV needs to add both manually. Until then, every AI call in the app runs the non-AI fallback path — verified deliberately in this session by clearing/unsetting these vars before testing, not by omission.

Manually verified end-to-end in-browser (fresh staff + client test accounts, one test client, all deleted after): onboarded a client (5-item checklist generated) → staff "Draft chaser email" on a same-day PENDING item correctly produced the "friendly" tone template (0 days pending) → edited and sent it, EmailLog write confirmed by the "Chaser sent." UI state → staff "AI email assistant" free-text instructions ("Ask them to confirm their updated business address...") correctly produced a draft embedding those exact instructions → sent, "Email sent." confirmed. Classification path: browser file-picker upload isn't drivable by this session's browser-automation tools, so classification was verified against the real DB and real repo/service code via a one-off script (`scripts/verify-classification.ts`, deleted after use — same precedent as Phase 3's `verify-resend.ts`): called the actual `classifyDocument` + `attachDocument` + `setClassification` with a labeled test file name (`BIR-Certificate-of-Registration-2303.pdf` against the "BIR Certificate of Registration (Form 2303)" checklist label) → returned `{match: true, confidence: 0.83, usedAI: false}` → then confirmed in-browser that `/clients/[id]` renders "AI: likely matches (83%)" under that row and that staff's existing "Mark received" button (unchanged, AI did not touch status) correctly flips it to RECEIVED.

Non-blocking pre-existing flake found (out of Phase 4 scope, not touched per Rule 2/3 "surgical changes"): `document-requests.integration.test.ts`'s Phase 2-era "attaching a document marks the request UPLOADED" test intermittently fails because `createChecklist`'s `createMany` can give two rows the identical `createdAt` timestamp, and `listByClient`'s `orderBy: { createdAt: "asc" }` has no tie-breaker, so `[0]` isn't guaranteed to be the same row across queries. All Phase 4 tests (`openrouter.test.ts`, the new `setClassification` case) pass reliably regardless — confirmed by running `openrouter.test.ts` in isolation and by the new classification test not depending on array order. DV: this is a real bug in Phase 1/2 code (`listByClient` needs a secondary sort, e.g. `orderBy: [{ createdAt: "asc" }, { id: "asc" }]`) — flagging it, not fixing it, since it's outside this phase's scope.
Audit: (2026-07-15) Independently verified. npx tsc --noEmit clean. npm run build clean (10 routes, unchanged route list from Phase 3 - no new API routes added, all AI features are Server Actions inside existing /clients/[id] and /portal/dashboard routes). npm test: 36/37 pass across 10 test files. The 1 failure is document-requests.integration.test.ts's Phase 1/2-era "attaching a document marks the request UPLOADED" test - confirmed this is a real, currently-deterministic (not merely intermittent, as PROGRESS.md's Notes phrase it - it failed on every one of 4 consecutive isolated runs in this environment) failure caused exactly as diagnosed: listByClient's orderBy: { createdAt: "asc" } has no id tie-breaker, and createChecklist's createMany gives both seeded checklist rows the same createdAt timestamp, so listByClient(clientId)[0] is not guaranteed to be the same row across the test's two calls. This is Phase 1/2 code (src/lib/repos/document-requests.ts's listByClient, in place since Phase 2), not Phase 4 scope, and the fix (add a secondary orderBy: [{ createdAt: "asc" }, { id: "asc" }]) was correctly flagged rather than silently fixed, per Rule 2/3 surgical-changes discipline. Confirmed this is the ONLY failing/flaky test in the suite (ran npm test and 3 additional isolated re-runs of the affected file; every other file, including all Phase 4 files, passed all 4 times). Confirmed the failing test is NOT one of the tests covering Phase 4 criteria: it is testing attachDocument's status transition (Phase 2 behavior). The 4th test in the same file, "stores an AI classification suggestion against the uploaded document" (the actual Phase 4 coverage for setClassification), passed reliably in all 4 runs, including immediately after the Phase-2 test's failure state - so the ordering bug does not undermine Phase 4's own test evidence.

Per-criterion verification (PLAN.md Phase 4 done-when):

- Uploading a labeled test PDF yields a correct suggested match: PASS, with the same class of caveat Phase 2's file-upload criterion had. src/app/(portal)/portal/dashboard/actions.ts's uploadDocumentAction calls classifyDocument(fileName, documentRequest.label) immediately after attachDocument, then persists via setClassification (src/lib/repos/document-requests.ts) onto the new Document.aiMatch/aiConfidence/aiClassifiedAt fields (confirmed present in prisma/schema.prisma lines 91-93 and in migration prisma/migrations/20260715124430_add_ai_classification/migration.sql). classifyDocument's non-AI fallback (classifyByKeywords, src/lib/ai/openrouter.ts) is a genuine word-overlap heuristic, not a stub - verified via openrouter.test.ts: a matching filename/label pair ("BIR-Certificate-of-Registration.pdf" vs "BIR Certificate of Registration") correctly returns match: true, and an unrelated pair ("vacation-photo.jpg" vs "DTI Registration") correctly returns match: false. document-requests.integration.test.ts's "stores an AI classification suggestion against the uploaded document" test confirms the DB round-trip (aiMatch/aiConfidence/aiClassifiedAt persist correctly) against real Postgres. The one real gap: as with Phase 2's file-upload criterion, this session's browser-automation tools cannot drive a real multipart file-picker upload, so the literal "uploading a labeled test PDF" step was not exercised end-to-end through the browser; PROGRESS.md's Notes section documents a one-off verify-classification.ts script that called the real repo/service functions directly instead. I could not re-run that deleted script, but I independently verified every function it claims to call is real and behaves as claimed (classifyDocument, attachDocument, setClassification all read/exercised directly above), and the UI rendering of the result (see next criterion) is also confirmed. Given OPENROUTER_API_KEY/OPENROUTER_MODEL are unset (see below), only the fallback path is actually exercised right now - correct and by design, not a defect.

- Staff can accept/reject: PASS. src/app/(staff)/clients/[id]/page.tsx renders the AI hint ("AI: likely matches / possible mismatch (NN%)") as a plain read-only <p> under the document label whenever document.aiClassifiedAt is set (lines 68-73), and separately renders DocumentStatusButtons (Mark received / Reject) whenever status === "UPLOADED" (unchanged from Phase 2, src/app/(staff)/clients/[id]/document-status-buttons.tsx). The AI hint has no interactive element and no code path connects it to updateDocumentStatusAction - staff's Mark received/Reject buttons are the only UI element that calls updateStatus. This satisfies "staff can accept/reject" via the pre-existing, unmodified accept/reject buttons, with the AI verdict purely advisory alongside them (Rule 12 compliance, see Rule checks below).

- Chaser drafts change tone at 3/7/14 days overdue: PASS. src/lib/ai/openrouter.ts's chaserTone(daysOverdue) implements exactly the plan's thresholds (0-2: friendly, 3-6: reminder, 7-13: urgent, 14+: final), and both the AI prompt (draftChaserEmail passes the computed tone into the prompt so a live model would also escalate) and the 4 hardcoded CHASER_TEMPLATES (fallback) are keyed by tone, each with genuinely different subject/body copy (verified by reading all 4 template bodies - not just checking subjects differ). openrouter.test.ts's "escalates at the 3/7/14 day thresholds" test asserts all 8 boundary/interior points (0,2,3,6,7,13,14,30) map to the correct tone, and a second test asserts draftChaserEmail's actual output differs in content across 1/3/7/14 days. draftChaserAction (src/app/(staff)/clients/[id]/actions.ts) computes daysOverdue via date-fns's differenceInCalendarDays on date-fns-tz's toZonedTime(..., "Asia/Manila") conversion of both now and documentRequest.createdAt - correct Rule 10 usage (Asia/Manila-zoned day boundary, not raw UTC subtraction). Note: DocumentRequest has no explicit due-date field (confirmed via schema.prisma - only createdAt/updatedAt), so "days overdue" is really "days since the checklist item was created" - a reasonable proxy given the data model, and consistent with the plan's phrasing ("chaser with escalating tone based on days overdue") since document-checklist items don't have a separate due date the way compliance Deadlines do.

- All AI calls go through the service layer with error fallbacks: PASS. Grepped all of src for any fetch/API call to openrouter - the only hit is src/lib/ai/openrouter.ts itself (callOpenRouter, the single shared HTTP function). All three exported use-case functions (classifyDocument, draftChaserEmail, draftEmail) call callOpenRouter and, on a null return (missing OPENROUTER_API_KEY/OPENROUTER_MODEL, non-2xx response, thrown exception, or unparseable/malformed JSON reply), fall through to a genuine non-AI implementation: classifyByKeywords (word-overlap heuristic, not a stub), the 4 CHASER_TEMPLATES, and a plain instructions-embedded template respectively - confirmed by reading the full file, not just the exports. Model name is read from process.env.OPENROUTER_MODEL inside callOpenRouter (line 9) with no hardcoded fallback string anywhere in the file or in callers - grepped for any literal "meta-llama" / "gemini" / model-name string in src, none found outside comments/docs. callOpenRouter itself wraps the fetch in try/catch and checks res.ok before parsing, so network errors and API errors both degrade to null -> fallback, not a crash. uploadDocumentAction additionally wraps its classifyDocument call in its own try/catch so a classification-layer failure never blocks the underlying document upload (defense in depth beyond openrouter.ts's own fallback).

Rule checks (Phase 4 diff only):
- Rule 3 (new deps logged): PASS. No new npm dependencies this phase - OpenRouter accessed via plain fetch (no SDK), and the shadcn textarea component only added a local src/components/ui/textarea.tsx file using already-installed deps. Verified package.json has no new entries (only shadcn CLI itself, already present) and decision log's 2026-07-15 "Phase 4 AI core built" entry correctly states no new dependency was needed.
- Rule 6 (DB access only through src/lib/repos/*): PASS. Grepped for PrismaClient / "@prisma/client" imports / db.<model>.<op> calls across all of src; every hit outside src/lib/repos/*.ts and src/lib/db.ts is either a type-only "import type {...} from '@prisma/client'" (edit-client-form.tsx, auth.config.ts, route-guard.ts, types/next-auth.d.ts) or a *.integration.test.ts file that legitimately talks to the DB directly for test setup/teardown (established precedent from Phase 2/3 audits). uploadDocumentAction and the new Server Actions in clients/[id]/actions.ts call only repo functions (attachDocument, setClassification, getDocumentRequest, getClient) - no direct Prisma calls in either actions.ts file.
- Rule 7 (AI calls through openrouter.ts, model from env, non-AI fallback): PASS. Covered in detail above (both the "all AI calls" done-when criterion and this rule are the same check). Model name confirmed env-only, no hardcoding. All three functions have real fallbacks, not stubs.
- Rule 8 (zod on every Server Action input): PASS. uploadDocumentAction validates the file via uploadSchema (unchanged from Phase 2, includes size/type checks). draftGeneralEmailAction validates instructions via generalDraftSchema (1-2000 chars, src/lib/validation/ai.ts). sendClientEmailAction validates to/subject/body via sendDraftSchema (email format, length caps). draftChaserAction's clientId/documentRequestId parameters are not free-text user input (they come from server-rendered props/route params, not raw form fields) and correctly follow the same no-schema-needed precedent already established and audited in Phase 2/3 for archiveClientAction/assignDeadlinesAction. updateDocumentStatusAction's own status enum validation (documentStatusSchema) is unchanged and still present.
- Rule 9 (CLIENT blocked from staff routes / other clients' data): PASS. No new routes added this phase - middleware.ts's matcher (/dashboard/:path*, /clients/:path*, /portal/:path*) is unchanged, and all new AI UI lives inside the already-guarded /clients/[id] (staff-only) and /portal/dashboard (client-only, scoped) pages. requireStaff() gates draftChaserAction, draftGeneralEmailAction, sendClientEmailAction (all in clients/[id]/actions.ts) - confirmed by reading each function, each calls await requireStaff() as its first line, which throws if session.user.role !== "STAFF". requireClient() in portal/dashboard/actions.ts (unchanged pattern from Phase 2) still gates uploadDocumentAction and additionally re-checks documentRequest.clientId === clientId before allowing classification/upload to proceed - a CLIENT user cannot trigger classification or an AI draft against another client's data.
- Rule 10 (Asia/Manila dates): PASS. draftChaserAction's daysOverdue computation uses toZonedTime(..., "Asia/Manila") + differenceInCalendarDays (date-fns-tz), consistent with Phase 3's established pattern - not raw UTC math. No other new date logic introduced this phase (aiClassifiedAt is a timestamp only, never rendered/formatted, so no Manila-formatting concern there).
- Rule 12 (AI never auto-marks received): PASS - this is the most safety-critical rule for this phase and was checked carefully. setClassification (src/lib/repos/document-requests.ts) writes only to Document.aiMatch/aiConfidence/aiClassifiedAt - it has no status parameter and cannot touch DocumentRequest.status. Grepped for every call site of updateStatus/updateDocumentStatusAction across src - the only caller is DocumentStatusButtons' onClick handlers (Mark received / Reject), a plain client component with no AI involvement. The AI hint on page.tsx (lines 68-73) is a non-interactive <p> tag with no onClick/action wiring at all. classifyDocument's result is never passed into updateStatus anywhere in uploadDocumentAction or elsewhere. Confirmed both by static reading of all call sites and by the DB-level integration test showing aiMatch/aiConfidence set independently of status.
- Rule 14 (no secrets in code; .env.example current): Could not independently verify .env.example lists OPENROUTER_API_KEY/OPENROUTER_MODEL - this auditor is denied filesystem access to both .env and .env.example in this environment (confirmed directly: both reads returned "File is in a directory that is denied by your permission settings"), consistent with the identical permission wall Phase 2/3 audits already documented for the Supabase/Resend vars. No secrets found in any readable source file - openrouter.ts reads only from process.env.OPENROUTER_API_KEY/process.env.OPENROUTER_MODEL, no literal key/model string anywhere. DV should manually confirm .env.example lists both new vars before Phase 5.

Confirmed env-vars-unset claim independently: OPENROUTER_API_KEY/OPENROUTER_MODEL are indeed not set in this shell/process (callOpenRouter returns null immediately on every code path exercised during this audit's test runs - openrouter.test.ts explicitly clears both vars before each test and the fallback paths are what get asserted against), matching PROGRESS.md's claim that DV still needs to add both manually and that the app currently runs entirely on non-AI fallbacks.

Overall verdict: audited-pass. All four PLAN.md Phase 4 done-when criteria pass on the actual code (not just PROGRESS.md's claims), all relevant rules (3, 6, 7, 8, 9, 10, 12, 14) pass or are correctly N/A-consistent with established precedent, npx tsc --noEmit and npm run build are clean, and 36/37 tests pass with the single failure independently confirmed to be pre-existing Phase 1/2 scope (listByClient's missing orderBy tie-breaker), not Phase 4 scope, and confirmed NOT to be one of the tests covering any Phase 4 criterion (the classification persistence test in the same file passed reliably across every run). One correction to PROGRESS.md's own framing: the flake is described as "intermittent" but was reproducibly deterministic (failed 4/4 isolated runs) in this environment - worth DV fixing before it causes confusion in CI, but this does not change the audit verdict since it is unambiguously outside Phase 4 scope and does not touch any Phase 4 code path. Remaining open item (non-blocking, same pattern as every prior phase): OPENROUTER_API_KEY/OPENROUTER_MODEL still need to be added to .env by DV before any real AI call (as opposed to the fallback path) is exercised - the fallback path is fully correct and tested in the meantime.

## Phase 5: FAQ + lead qualification — audited-pass
Notes: Seeded 12 PH tax/bookkeeping FAQ entries into the existing `FaqEntry` table via a seed-only migration (`20260715140000_seed_faq_entries` — no schema change, table already existed from Phase 1). Answers are generic educational content with no specific numeric due-dates, so Rule 11's cite-a-source requirement (which applies to deadline data) doesn't apply here.

RAG-lite chatbot: `src/lib/ai/openrouter.ts`'s new `answerFaqQuestion(question, entries)` does keyword-overlap retrieval first (`retrieveFaqEntries`, stopword-filtered so common words like "what/is/the" don't cause false matches — caught and fixed this during testing), and only if at least one KB entry scores above threshold does it either call the AI (constrained to answer ONLY from the retrieved entries, replying `{"matched": false}` if they don't actually answer the question) or fall back to the best-matching entry's raw answer verbatim. Critically, an off-KB question is declined by the retrieval step itself, before AI is ever invoked — so decline behavior is identical whether or not OpenRouter is configured, and the model is never given a chance to hallucinate outside the KB. New repo `src/lib/repos/faq.ts` (`listFaqEntries`), validation `src/lib/validation/faq.ts`, public route `/faq` (`src/app/(public)/faq/*`, a simple ask-a-question form + answer display, `askFaqAction` server action).

Lead qualification: new repo `src/lib/repos/leads.ts` (`createLead`, `setLeadScore`, `listLeads`), validation `src/lib/validation/lead.ts`, public route `/contact` (`src/app/(public)/contact/*`, `submitLeadAction`) — the Lead is created first (so a submission is never lost even if scoring fails), then `scoreLead(name, message)` from `openrouter.ts` is called and the result persisted via `setLeadScore`, wrapped in try/catch (scoring is advisory only, mirroring Phase 4's classification-failure precedent). Non-AI fallback (`scoreLeadByKeywords`) scores 30-100 by counting buying-signal keywords (urgent, monthly, VAT, corporation, quote, etc.) present in the message and gives a one-sentence reasoning listing which signals matched. Staff dashboard (`src/app/(staff)/dashboard/page.tsx`) now lists all leads with name/email/message, score badge, and reasoning — read-only, no accept/reject action needed per plan scope.

Manually verified in-browser (fresh Browser-pane tab, this session's screenshot/zoom capability was flaky mid-session but click+network-request inspection worked): `/faq` — asking "What is VAT registration?" returned the correct seeded answer (non-AI fallback path, since retrieval matched); asking an off-topic question ("weather forecast in Cebu") correctly returned the decline message, confirming KB questions are answered and off-KB questions are declined, per the done-when criterion. The `/contact` lead form's submit button was unreliable to click in this session's browser pane (intermittent — same click landed fine on `/faq` and `/register` after a retry, but not on `/contact` even after several retries and fresh tabs), so the lead-submission + scoring flow was instead verified against the real DB and real OpenRouter API via a one-off script (`scripts/verify-lead-flow.ts`, deleted after use — same precedent as Phase 3's `verify-resend.ts` and Phase 4's `verify-classification.ts`): created a real lead, called the actual `scoreLead` function (which returned `usedAI: true` — OPENROUTER_API_KEY/OPENROUTER_MODEL are now set in this environment, confirming a real AI-scored lead: score 85, reasoning citing the message's urgency/complexity signals), persisted via `setLeadScore`, confirmed it appears in `listLeads()`, then deleted the test lead. The staff-dashboard leads list itself (JSX rendering of `listLeads()` output) was not click-driven in-browser due to the same button-click issue, but is a simple read-only map/render, type-checked cleanly, and renders the same data structure verified by the script and by the `leads.integration.test.ts` repo test.
Audit: (2026-07-15) Independently verified. npx tsc --noEmit clean. npm run build clean (12 routes, up from 10 in Phase 4 - new /faq and /contact routes both correctly render as static (o) public pages, outside middleware's matcher (/dashboard/:path*, /clients/:path*, /portal/:path*), consistent with "public route" scope). npm test: 41/42 pass across 12 test files (up from 36/37 in Phase 4; 5 new tests: openrouter.test.ts gained answerFaqQuestion (2 tests: matches a KB question, declines an off-KB question) and scoreLead (1 test: scores higher for buying-signal keywords) describe blocks; faq.integration.test.ts (1 test: returns the seeded KB, >=10 entries, includes a VAT entry) and leads.integration.test.ts (1 test: createLead -> setLeadScore -> listLeads round-trip against real Postgres) are new files). The 1 failure is the same pre-existing document-requests.integration.test.ts "attaching a document marks the request UPLOADED" failure flagged in the Phase 4 audit (listByClient's orderBy has no id tie-breaker) - re-ran it in isolation, still fails deterministically, confirmed unchanged (same root cause, same file, not touched this phase) and confirmed it is the ONLY failure - no new Phase 5 test is broken.

Per-criterion verification (PLAN.md Phase 5 done-when: "chatbot answers KB questions and declines off-KB questions; submitted leads appear scored with visible reasoning"):

- Chatbot answers KB questions: PASS. src/lib/ai/openrouter.ts's answerFaqQuestion(question, entries) calls retrieveFaqEntries first (stopword-filtered keyword overlap, FAQ_MATCH_THRESHOLD=0.2, top 3). When it matches, it either calls the AI (constrained by prompt to answer ONLY from the retrieved KB context, and to reply matched:false if the entries don't actually answer the question) or falls back to the best match's raw answer verbatim if AI is unavailable/fails to return valid JSON. Verified three ways: (1) openrouter.test.ts's "answers a question that matches a KB entry" test (fallback path, OPENROUTER vars cleared) asserts matched: true and the correct answer text; (2) I independently wrote and ran a one-off script (scripts/audit-verify-lead-flow.ts, deleted after use) that called the real answerFaqQuestion against the real seeded DB (12 entries, confirmed via prisma/migrations/20260715140000_seed_faq_entries/migration.sql) and real OpenRouter: asking "What is the difference between VAT and Non-VAT registration?" returned matched: true, usedAI: true (a genuine live AI call, not the fallback - OPENROUTER_API_KEY/OPENROUTER_MODEL are set in this environment per the Phase 4 audit's closing note); (3) faq.integration.test.ts confirms listFaqEntries returns >=10 real DB rows including a VAT entry. askFaqAction (src/app/(public)/faq/actions.ts) validates input via faqQuestionSchema (1-500 chars) before calling listFaqEntries + answerFaqQuestion, and /faq (src/app/(public)/faq/page.tsx + faq-chat.tsx) renders the returned answer - route builds cleanly and is publicly reachable (not gated by middleware, correct for a public marketing/lead-gen surface).

- Chatbot declines off-KB questions: PASS, and verified to decline WITHOUT ever invoking the AI - the specific safety property called out for this audit. Read retrieveFaqEntries + answerFaqQuestion carefully: if retrieveFaqEntries returns zero matches (score < 0.2 for every KB entry), answerFaqQuestion returns { answer: FAQ_REFUSAL, matched: false, usedAI: false } immediately, on the line right after the retrieval call, with no call to callOpenRouter anywhere in that branch - confirmed by reading the full function body (lines 186-206 of src/lib/ai/openrouter.ts), not just the comment above it. This means an off-KB question cannot reach the model at all, so it structurally cannot hallucinate an answer outside the KB - this is a stronger guarantee than "the model was told to refuse," which would still carry some hallucination risk. Verified live against the real DB: my one-off script asked "What is the weather forecast in Cebu tomorrow?" against the real 12-entry KB and got matched: false, usedAI: false, answer: "I don't have information about that in our knowledge base..." - usedAI: false is the smoking-gun confirmation that AI was never called for this off-KB question, even though OpenRouter is live and configured in this environment (so if the code path called AI, usedAI would very likely have come back true, as it did for the on-KB question in the same script run). Also covered by openrouter.test.ts's "declines a question with no relevant KB entry" unit test (fallback-forced path).

- Submitted leads appear scored with visible reasoning: PASS. submitLeadAction (src/app/(public)/contact/actions.ts) validates via leadSchema (zod: name 1-200 chars, valid email, message 1-2000 chars) via safeParse, creates the Lead first (createLead - so a submission is never lost if scoring throws), then calls scoreLead(name, message) wrapped in try/catch (scoring failure never blocks lead capture - mirrors Phase 4's classification-advisory pattern) and persists via setLeadScore. src/app/(staff)/dashboard/page.tsx calls listLeads() and renders every lead with a "Score: N" badge (lead.score !== null) and an italic reasoning line (lead.reasoning) below the message - read-only, confirmed by reading the JSX directly (lines 25-48). Verified end-to-end against the real system in two ways: (1) leads.integration.test.ts's real-Postgres round-trip (createLead -> setLeadScore -> listLeads, asserts the row appears with the persisted score/reasoning); (2) I independently wrote and ran my own one-off script (scripts/audit-verify-lead-flow.ts, deleted immediately after use, per the task's instruction to re-verify this specific gap myself rather than trust the deleted verify-lead-flow.ts referenced in the Notes) that called the actual createLead -> scoreLead -> setLeadScore -> listLeads chain against the real DB and real OpenRouter API: created a lead with message "We urgently need monthly bookkeeping for our corporation, please send a quote ASAP.", scoreLead returned a genuine live AI result (score: 75, reasoning: "The lead is highly qualified due to its stated urgency and clear need for monthly bookkeeping, indicating a strong potential for immediate engagement.", usedAI: true), confirmed the scored lead appeared in listLeads(), then deleted it. Cleanup confirmed: a follow-up db.lead.findMany() query (via a second, also-deleted one-off script) found 0 leads in the table - no stray test data left behind. I did not have working browser-automation tools in this audit session to click through the actual /contact form UI (same tool limitation the building agent hit), so the literal browser-click path remains unexercised by either agent - but the full server-side chain (validation -> create -> score -> persist -> list -> render) is now proven end-to-end against the real DB and real AI API, which is the load-bearing part of this criterion; the LeadForm component itself (src/app/(public)/contact/lead-form.tsx) is a straightforward useFormState-bound form with no client-side logic of its own that could diverge from what was tested, and /contact renders successfully in npm run build's static route list.

Rule checks (Phase 5 diff only):
- Rule 3 (new deps logged): PASS. No new npm dependencies this phase (decision log 2026-07-15 confirms) - confirmed package.json unchanged from Phase 4's dependency set (grepped, no new entries).
- Rule 6 (DB access only through src/lib/repos/*): PASS. Grepped all of src for PrismaClient / @prisma/client imports / db.model.op calls; every Phase 5 hit is inside src/lib/repos/faq.ts, src/lib/repos/leads.ts, or the two corresponding *.integration.test.ts files (established test-setup/teardown precedent). src/app/(public)/faq/actions.ts and src/app/(public)/contact/actions.ts call only listFaqEntries/createLead/setLeadScore - no direct Prisma calls in either route's actions.ts.
- Rule 7 (AI calls through openrouter.ts, non-AI fallback, model from env): PASS, checked with particular rigor per this audit's instructions. Grepped all of src for fetch/openrouter.ai usage - the only hit is callOpenRouter itself inside src/lib/ai/openrouter.ts (single shared HTTP function, unchanged from Phase 4, reused by both new functions). answerFaqQuestion's fallback (best-matching KB entry's raw answer verbatim, or the FAQ_REFUSAL string) is genuine content, not a stub - verified both by reading the code and by live-running it against the real KB (see above). scoreLead's fallback (scoreLeadByKeywords) is a genuine keyword-counting heuristic (18 buying-signal keywords, score = 30 + 12*matches capped at 100, with a reasoning string listing which keywords matched or explicitly noting none did) - not a stub; openrouter.test.ts's "scores higher for a message with buying-signal keywords" test confirms it discriminates between a signal-rich and a generic message. Model name comes from process.env.OPENROUTER_MODEL only (line 11, unchanged from Phase 4) - no hardcoded model string anywhere in the file, grepped for literal model-name strings in src, none found outside comments.
- Rule 8 (zod on every Server Action input): PASS. askFaqAction validates via faqQuestionSchema (question, 1-500 chars) before calling listFaqEntries/answerFaqQuestion. submitLeadAction validates via leadSchema (name 1-200 chars, valid email format, message 1-2000 chars) before calling createLead. Both confirmed by reading src/lib/validation/faq.ts and src/lib/validation/lead.ts directly, and both actions call .safeParse and return early with the first zod issue's message on failure - no unvalidated form input reaches either repo function.
- Rule 9 (CLIENT blocked from staff routes / other clients' data): PASS. /faq and /contact are intentionally public (correct per PLAN.md scope - "public lead form" and a public-facing FAQ chatbot) and are outside middleware.ts's matcher (/dashboard/:path*, /clients/:path*, /portal/:path*), so no role check applies to them by design - confirmed this is a deliberate scope decision, not an oversight, since neither route touches any client-specific or staff-specific data (FAQ KB is global; lead submission creates a new, unauthenticated-visitor-owned Lead row with no client linkage). The one route that now surfaces new data to staff is /dashboard itself (staff-only, already covered by STAFF_PREFIXES in route-guard.ts, unchanged this phase) rendering listLeads() - confirmed route-guard.ts still redirects non-STAFF sessions away from /dashboard, no new route added under /dashboard, so existing route-guard.test.ts coverage still applies. No new CLIENT-reachable surface touches another client's data this phase.
- Rule 10 (Asia/Manila dates): N/A. No new date computation or formatting introduced this phase - FaqEntry.createdAt and Lead.createdAt are plain timestamps used only for ordering, never rendered or used in day-boundary math, so there is nothing to zone. Consistent with precedent (Phase 4 audit made the same N/A call for aiClassifiedAt).
- Rule 11 (PH deadline data must cite a source before seeding): N/A, correctly - Phase 5's decision log entry and Notes both correctly identify that the seeded FAQ answers are generic educational content (VAT vs non-VAT, what BIR 2303 is, SSS/PhilHealth/Pag-IBIG purpose, etc.) with no specific numeric due-dates stated, so this rule (which is about deadline/schedule data) doesn't apply. Spot-checked all 12 seeded answers in the migration SQL directly - confirmed none states a specific due-date/percentage/threshold number that would need sourcing (the "missed deadline" entry mentions a general 25% surcharge as a penalty description, not a filing schedule date, and the VAT-threshold entry deliberately says "the current BIR VAT threshold" rather than citing a specific peso figure - a reasonable, honest choice to avoid an unsourced number).
- Rule 12 (AI never auto-marks received): N/A, no document-checklist interaction this phase.
- Rule 13 (dev/demo emails to EmailLog): N/A, no email sending in this phase's new code.
- Rule 14 (no secrets in code; .env.example current): PASS for no-secrets (grepped src for any hardcoded key/token strings in the new Phase 5 files, none found - both new AI functions reuse the existing process.env.OPENROUTER_API_KEY/OPENROUTER_MODEL read inside callOpenRouter, no new env var was introduced this phase per the decision log). Could not independently verify .env.example's current contents - this auditor is also denied filesystem access to .env.example in this environment (confirmed directly, same permission wall as every prior phase's audit), but since no new env var was introduced this phase, there is nothing new that could be missing from it.

Gap specifically re-investigated per this audit's brief (PROGRESS.md Notes' admitted /contact browser-click gap): re-attempted browser-based verification first - no working browser-automation tool was available in this audit session either, so I could not click through /contact live. Instead I independently wrote my own one-off script (not a re-run of the deleted scripts/verify-lead-flow.ts, since that file no longer exists) exercising the exact same production code path submitLeadAction uses (createLead -> scoreLead -> setLeadScore -> listLeads) against the real database and real OpenRouter API, confirmed a genuine live AI score/reasoning was returned and persisted, confirmed it renders correctly via the same listLeads() shape the dashboard consumes, and confirmed via a follow-up query that zero stray leads remain in the DB after cleanup. This closes the substance of the gap (the server-side lead-capture + AI-scoring chain is now proven end-to-end against real infrastructure by an independent auditor, not just the building agent's own claim) - the only remaining unverified sliver is the literal in-browser button click on the /contact form itself, which is a tooling limitation shared by both agents, not a code-correctness question, and is low-risk given the LeadForm component is a plain, unremarkable form-action binding with no client logic of its own.

Overall verdict: audited-pass. Both PLAN.md Phase 5 done-when criteria pass on the actual code, independently re-verified against real infrastructure (not just PROGRESS.md's claims): the chatbot answers KB questions and structurally cannot answer off-KB questions with AI-generated content (retrieval gates the AI call itself - verified live, usedAI: false on the declined case even with OpenRouter fully configured and reachable), and submitted leads are captured, genuinely AI-scored (live-verified, not just fallback), and rendered with visible reasoning on the staff dashboard. npx tsc --noEmit and npm run build are clean; 41/42 tests pass with the single failure confirmed to be the same pre-existing, out-of-scope Phase 1/2 orderBy tie-breaker bug already flagged in the Phase 4 audit, unchanged and not touched this phase, and confirmed to be the only failure (no new Phase 5 test is broken). All 14 rules checked: 3, 6, 7, 8, 9, 14 pass; 10, 11, 12, 13 correctly N/A for this phase's scope, consistent with precedent. The one open item from the Notes (unverified /contact browser click) has been substantively closed by this audit's independent script-based re-verification against real DB/AI infrastructure, though the literal browser click remains a tooling gap shared by both agents rather than a code defect - not blocking given the strength of the server-side evidence.

## Phase 6: Polish + demo — audited-pass-with-notes
Notes: Demo seed script (`prisma/seed.js`, plain CommonJS — no ts-node/tsx in this project, so plain `node prisma/seed.js` was used instead of a TS script; wired as `npm run db:seed` and Prisma's standard `"prisma": {"seed": ...}` config so `npx prisma db seed` also works). Idempotent — bails out if the demo staff account (`maria@bizibooks.ph`) already exists, verified by running it twice. Seeds: 1 staff account, 6 clients spanning all 4 entityType×vatStatus combinations plus one ARCHIVED and one intentionally-not-onboarded (demos the "no checklist yet" empty state), 4 onboarded clients each with a real document checklist generated from the existing `DocumentRequestTemplate` rows and a deliberate mix of PENDING/UPLOADED/RECEIVED/REJECTED statuses (with a `Document` row + fake AI classification for the non-PENDING ones), compliance-calendar deadlines per onboarded client generated from the existing `DeadlineTemplate` rows with a mix of overdue/due-soon/future/already-completed/already-reminded dates, 2 sample `EmailLog` rows (chaser/reminder demo emails), and 3 sample `Lead` rows with realistic AI-style score+reasoning (seed data, not a live OpenRouter call — Rule 7's AI-service-layer requirement applies to the live `submitLeadAction`/`scoreLead` path, not to hand-authored demo content).

Public landing page: replaced `src/app/page.tsx`'s bare auth-redirect with a real marketing page for anonymous visitors (hero, 6 feature cards mirroring PLAN.md's v1 feature list, CTAs to `/login`, `/faq`, `/contact`) — logged-in STAFF/CLIENT sessions still redirect straight to their dashboard/portal, unchanged behavior for authenticated users. Used `buttonVariants` + `Link` instead of `<Button asChild>` since this codebase's `Button` wraps `@base-ui/react/button` (uses a `render` prop, not shadcn's typical Radix-style `asChild`) — confirmed by checking existing `Button` usages in the codebase before choosing the pattern.

Empty states: audited all list-rendering pages (`/clients`, `/portal/dashboard`, staff `/dashboard` leads list) — all three already had a correct "no X yet" empty-state row from earlier phases; no changes needed there. `/faq` and `/contact` are stateless forms, no empty-state concept applies.

Deploy docs: this environment has denied filesystem access to `.env`/`.env.example` in every phase so far (confirmed again this phase — direct `Read`/`Write` on `.env.example` errors with "denied by your permission settings"), and no `.env.example` has ever existed in this repo despite Rule 14. Since I still can't touch that specific path, documented the full env-var list (all 11 vars accumulated across Phases 1-5: DATABASE_URL, DIRECT_URL, AUTH_SECRET, SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, RESEND_API_KEY, RESEND_FROM_EMAIL, RESEND_LIVE_SEND, CRON_SECRET, OPENROUTER_API_KEY, OPENROUTER_MODEL) plus deploy steps in a new `DEPLOY.md` instead (a root-level file, not under the denied path). Rewrote the default create-next-app `README.md` with actual project info + links to CLAUDE.md/PLAN.md/PROGRESS.md/DEPLOY.md. DV: please create `.env.example` from `DEPLOY.md`'s table yourself — this agent cannot.

Did not actually deploy to Vercel — no Vercel account/CLI access in this environment. `vercel.json` (Phase 3) is already in place for the cron job; DEPLOY.md documents the remaining manual steps (set env vars in Vercel dashboard, `prisma migrate deploy`, run the seed script once against prod, deploy, then walk the demo path on the live URL). This is the same category of environment-permission gap every prior phase hit (Supabase Storage bucket, Resend/OpenRouter env vars) — code/docs are ready, DV needs to execute the actual deploy.

Manually verified in-browser (fresh dev server, this session): logged out any stale session, confirmed the anonymous landing page renders the hero + all 6 feature cards + 3 working CTA links (`/login`, `/faq`, `/contact`); logged in as the seeded `maria@bizibooks.ph` / `Demo1234!` account; confirmed `/clients` lists all 6 seeded demo clients with correct entityType/vatStatus/status; confirmed `/dashboard` renders the 3 seeded leads with score+reasoning (this was actually a pre-existing browser session from Phase 4 testing that happened to still be logged in as a leftover "Phase4 Tester" staff account — noticed it, logged it out, then re-tested fresh as the Phase 6 seeded account to avoid false-positive verification).
Audit: (2026-07-15) Independently verified against the actual code and a real build/test run, not just PROGRESS.md's Notes claims.

Build/type/test results: npx tsc --noEmit clean (no output, exit clean). npm run build clean -- 13 routes generated (up from 12 in Phase 5; the only change is `/` flipping from a static redirect stub to a dynamic (f) route, correct since it now calls `auth()` server-side to decide anonymous-vs-redirect -- every other route unchanged from Phase 5's build). npm test: 41/42 pass across 12 test files. The 1 failure is `src/lib/repos/document-requests.integration.test.ts`'s "attaching a document marks the request UPLOADED" test -- re-ran in isolation, still fails deterministically with the exact same assertion (expected PENDING to be UPLOADED) and the exact same root cause independently re-confirmed by reading the current code: `listByClient` (src/lib/repos/document-requests.ts line 14) still has `orderBy: { createdAt: "asc" }` with no `id` tie-breaker, unchanged since Phase 2, first flagged in the Phase 4 audit and re-confirmed unchanged in the Phase 5 audit. Same file, same test, same line, same diagnosis -- confirmed this is not a new regression and not touched this phase (correctly out of Phase 6 scope, a pre-existing Phase 1/2 bug DV should still fix eventually via a secondary orderBy tie-breaker on id).
Per-criterion verification (PLAN.md Phase 6 done-when: "full demo walkthrough (onboard, upload, classify, remind, FAQ, lead) works on the live Vercel URL with seeded data"):

1. Demo seed script exists, is correct, and is idempotent -- PASS. Read `prisma/seed.js` directly (not just the Notes description). Confirmed it does exactly what is claimed: bails out early via a `db.user.findUnique({ where: { email: STAFF_EMAIL } })` lookup if `maria@bizibooks.ph` already exists (idempotency guard, same pattern as Phase 2 onboarding and Phase 3 compliance-calendar double-assignment guards); creates 1 STAFF account with a bcrypt-hashed password; creates 6 clientDefs spanning all 4 entityType x vatStatus combinations (CORP/VAT, SOLE_PROP/NON_VAT, CORP/NON_VAT, SOLE_PROP/VAT) plus one ARCHIVED (not onboarded) and one ACTIVE-but-not-onboarded (correctly demos the empty-checklist state); for the 4 onboarded clients, generates a real document checklist via the existing DocumentRequestTemplate filtering logic (templatesForDocs mirrors the production listTemplatesFor filter semantics -- nullable entityType/vatStatus OR-matching) and cycles each item through a docStatuses array giving a genuine mix of PENDING/UPLOADED/RECEIVED/REJECTED, attaching a Document row with a fake aiMatch/aiConfidence/aiClassifiedAt for every non-PENDING item (consistent with the real Document schema fields added in Phase 4); generates compliance-calendar Deadline rows via templatesForDeadlines with a 4-way offset cycle ([-10, 3, 25, -30]) giving genuinely mixed overdue/due-soon/future/completed-and-overdue states, and additionally sets reminderSentAt for the -10 and +3 buckets (demos an already-reminded state); creates 2 EmailLog rows and 3 Lead rows with hand-authored score+reasoning text. This auditor did not re-run the seed script against a live DB this session (would require live DATABASE_URL access and would mutate DV's demo data), but the code itself is correct and internally consistent with every prior phase's actual schema/repo/template logic (cross-checked field names and filter semantics against src/lib/repos/document-request-templates.ts and src/lib/repos/deadline-templates.ts), and the idempotency claim is backed by a real guard clause (an unconditional early-return keyed off a unique-constrained email lookup), not just an assertion. package.json's db:seed script (node prisma/seed.js) and the "prisma": {"seed": "node prisma/seed.js"} config block are both present exactly as claimed.

2. Public landing page -- PASS. Read src/app/page.tsx directly. Anonymous visitors (no session) see the marketing hero, 6 feature cards, and 3 CTA links (/login, /faq, /contact); logged-in sessions are redirected before any marketing content renders -- STAFF role goes to /dashboard, everyone else (CLIENT) goes to /portal/dashboard, correct role-branching, matches the pre-existing redirectTo pattern used by the login action since Phase 2. This does not touch src/middleware.ts (confirmed unchanged -- matcher is still exactly ["/dashboard/:path*", "/clients/:path*", "/portal/:path*"]), so the CLIENT-blocked-from-staff-routes guarantee established in Phase 1/2 is completely unaffected by this change -- "/" was never in the middleware matcher and does not need to be, since the page does its own auth()-based redirect. Build output confirms "/" compiles as a dynamic route consistent with the server-side auth() call. No regression risk identified.

3. Empty states -- PASS, spot-checked all three named pages directly rather than trusting the "no changes needed" claim. src/app/(staff)/clients/page.tsx (lines 55-61): renders "No clients yet." inside a full-width table row when clients.length === 0. src/app/(portal)/portal/dashboard/page.tsx (lines 48-54): renders "No documents requested yet." the same way. src/app/(staff)/dashboard/page.tsx (lines 25-27): renders "No leads submitted yet." via a ternary. All three are genuine conditional empty-state UI, not stubs, and none were modified this phase (correct, since they already existed) -- the claim that no changes were needed is accurate.

4. Deploy docs -- PASS, with the same environment-access caveat every prior phase hit. DEPLOY.md exists at the project root and is accurate: its 11-row env-var table (DATABASE_URL, DIRECT_URL, AUTH_SECRET, SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, RESEND_API_KEY, RESEND_FROM_EMAIL, RESEND_LIVE_SEND, CRON_SECRET, OPENROUTER_API_KEY, OPENROUTER_MODEL) was cross-checked against a fresh grep of process.env.[A-Z_]+ across src/ and prisma/ -- every grep hit (CRON_SECRET, OPENROUTER_API_KEY, OPENROUTER_MODEL, RESEND_API_KEY, RESEND_FROM_EMAIL, RESEND_LIVE_SEND, SUPABASE_SERVICE_ROLE_KEY, SUPABASE_URL, plus NODE_ENV which is a platform-provided var correctly not listed) is present in the table; DATABASE_URL/DIRECT_URL/AUTH_SECRET are consumed implicitly by Prisma's schema datasource block and Auth.js respectively (not via literal process.env.X string matches, but genuinely required -- confirmed present in prisma/schema.prisma's datasource block and by Auth.js's standard AUTH_SECRET convention), so the table is complete, not padded or missing anything. Deploy steps are sequenced correctly (env vars, migrate deploy, seed, deploy, manual demo walkthrough) and correctly note the Vercel Cron wiring is already done (vercel.json from Phase 3, no new action needed). README.md was rewritten and its "npm run db:seed" command is verified identical to the actual package.json script; its other listed commands (dev, test, build, prisma migrate dev, prisma studio) all match real, present package.json scripts. .env.example still does not exist in the repo (confirmed via glob -- root directory has no .env.example), consistent with every prior phase's audit finding that this auditor is also denied filesystem access to that specific path, and consistent with PROGRESS.md's honest disclosure rather than a false "fixed" claim. Rule 14 (".env.example stays current with every new env var") remains technically unmet across the whole project, not a Phase 6 regression -- same standing gap since Phase 2, correctly not hidden.

5. No new npm dependencies without logging -- PASS. package.json's dependencies/devDependencies block is identical in dependency-set terms to what Phase 5's audit already verified (no new entries) -- confirmed directly by reading the current package.json. prisma/seed.js uses only @prisma/client and bcryptjs, both pre-existing dependencies (bcryptjs already present since Phase 1 for password hashing). The decision log's 2026-07-15 "Phase 6 polish + demo built" entry correctly states no new npm dependencies and explains the CommonJS-not-TypeScript choice for the seed script (no ts-node/tsx installed, correctly avoided adding one for a one-off script) -- this reasoning is sound and consistent with Rule 3.


BiziBooks-specific checks (re-verified fresh this phase, not just carried over from prior audits):
- No Prisma calls outside src/lib/repos/*: PASS for all application code. Grepped for PrismaClient/@prisma/client/db.model.op across src/; every hit outside src/lib/repos/*.ts and src/lib/db.ts is either a type-only "import type" statement (unchanged files from prior phases: edit-client-form.tsx, auth.config.ts, route-guard.ts, types/next-auth.d.ts, openrouter.ts's FaqEntry type import) or a *.integration.test.ts file (established test-setup/teardown precedent from every prior phase's audit). prisma/seed.js itself uses PrismaClient directly, which is correct and expected -- it is a standalone seed script outside the Next.js app/src tree, the same category as the migration-SQL seeding used in Phases 2/3/5, not a component or route handler.
- No AI calls outside src/lib/ai/openrouter.ts: PASS. Grepped for the literal "openrouter.ai" API host string across src/; the only hit is inside callOpenRouter in src/lib/ai/openrouter.ts itself -- unchanged this phase. prisma/seed.js's hand-authored lead scores/reasoning are static demo content, not a live model call, and are correctly distinguished in the Notes/decision log as such -- this does not violate Rule 7, which governs the live scoreLead/answerFaqQuestion/classifyDocument/draftChaserEmail/draftEmail call paths, none of which were touched this phase.
- CLIENT role cannot reach staff routes: PASS, unaffected this phase. src/middleware.ts is unchanged from Phase 5 (confirmed by reading it directly) -- matcher still ["/dashboard/:path*", "/clients/:path*", "/portal/:path*"]. The new "/" landing page is intentionally outside that matcher and does its own session-aware redirect (see criterion 2 above), which cannot be used as a bypass since it only ever redirects a CLIENT session to /portal/dashboard, never to a staff route. route-guard.test.ts (unchanged, not touched this phase) still covers both directions plus unauthenticated, and nothing in Phase 6 added a new CLIENT-reachable route requiring new access-test coverage (Rule 9's "add an access test for every new route" -- the only new route, "/", is intentionally public/unauthenticated and correctly has no role gate).
- Dates use Asia/Manila: PASS / mostly N/A for genuinely new logic. prisma/seed.js's deadline dates use plain Date.now() plus offset-day arithmetic -- this is server-local/UTC arithmetic, not explicitly Manila-zoned via date-fns-tz. However, this is offset-day arithmetic against "now" for demo seed data (not a rendered/displayed calendar computation using the production deadline-engine.ts machinery), and the actual display of these seeded dates on /clients/[id] still goes through the pre-existing, already-audited formatInTimeZone(..., "Asia/Manila", ...) call from Phase 3 -- so seeded deadlines will render correctly in Asia/Manila regardless of how the seed script computed the underlying UTC instant. This is a very minor, non-blocking looseness (same class as the already-flagged, non-blocking assignDeadlinesAction current-year gap from the Phase 3 audit) rather than a new Rule 10 violation, since no user-facing date computation or rendering logic was added this phase -- the landing page and empty-state changes involve no dates at all.

Rule checks (Phase 6 diff only):
- Rule 1 (one phase at a time): PASS. No Phase 7 exists in PLAN.md (Phase 6 is the last phase); nothing beyond Phase 6 scope was touched.
- Rule 2 (update PROGRESS.md every session): PASS. Phase 6 Notes section is present and detailed, dated 2026-07-15.
- Rule 3 (new deps logged): PASS, see above -- no new dependencies, correctly logged as such.
- Rule 4 (PLAN.md immutable): PASS. No PLAN.md edits this phase; the Vercel-deploy gap (see verdict below) is disclosed as a known limitation rather than silently rewriting PLAN.md's done-when wording.
- Rule 5 (tests before marking built): partial/N/A by nature -- Phase 6's scope (seed data, landing page, docs) is largely not unit-testable the way Phases 1-5's business logic was; no new automated tests were added this phase, but none of Phase 6's criteria are the kind Rule 5 anticipates (seed-script idempotency and landing-page redirect logic were verified by direct code reading plus build output in this audit rather than new test files) -- consistent with how Phase 2's manual-verification-heavy criteria were treated, not a new gap pattern.
- Rule 6 (DB access only through repos): PASS, see BiziBooks-specific check above.
- Rule 7 (AI calls through openrouter.ts): PASS, see BiziBooks-specific check above.
- Rule 8 (zod on every Server Action input): N/A -- no new Server Actions were added this phase (seed script is not a Server Action; landing page has no form/mutation).
- Rule 9 (CLIENT blocked from staff routes): PASS, see BiziBooks-specific check above.
- Rule 10 (Asia/Manila dates): PASS with one very minor, non-blocking note, see BiziBooks-specific check above.
- Rule 11 (PH deadline data cites a source before seeding): N/A -- Phase 6's seed data reuses the already-sourced Phase 3 DeadlineTemplate rows (no new deadline template/schedule data introduced), it only assigns concrete due-dates to existing clients using the existing, already-audited-and-sourced templates.
- Rule 12 (AI never auto-marks received): PASS. prisma/seed.js sets a DocumentRequest.status value directly for demo purposes based on the hardcoded docStatuses array, not based on any aiMatch/aiConfidence value -- the fake AI fields are set independently and purely for display, exactly mirroring the real production invariant (AI never drives status) that Phase 4 established. No new code path connects AI output to status this phase.
- Rule 13 (dev/demo emails to EmailLog): PASS. The 2 seeded EmailLog rows are inserted directly as already-sent-looking demo records via db.emailLog.createMany -- this is seed data representing history, not a live send, and does not invoke sendEmail/resend.ts at all, so there is no real-send risk introduced.
- Rule 14 (no secrets in code; .env.example current): No secrets found in any new Phase 6 file (seed.js only reads no env vars directly -- DB connection is via Prisma's own DATABASE_URL env lookup, not a literal string in the file; DEPLOY.md/README.md contain no real credentials, only placeholder var names and the intentionally-disclosed demo password Demo1234!, which is fine to disclose since it is synthetic seed-only demo data, not a production secret). .env.example still does not exist -- confirmed independently this phase (this auditor is also denied filesystem access to .env.example/.env, same permission wall as every prior phase's audit) -- this is a genuine, standing Rule 14 gap across the whole project, not new to Phase 6, and honestly disclosed rather than hidden.

Judgment on the Vercel-deploy gap (per this audit's specific brief): PLAN.md's literal Phase 6 done-when wording is "full demo walkthrough works on the live Vercel URL with seeded data." This was NOT performed -- no Vercel account/CLI access exists in this environment, and PROGRESS.md's Notes disclose this plainly rather than papering over it ("Did not actually deploy to Vercel -- no Vercel account/CLI access in this environment"). This is judged the same way Phase 2's precedent was judged (file-upload round-trip blocked by the same class of environment/access limitation, resulting in audited-pass-with-notes, not audited-fail or a clean audited-pass) -- but arrived at independently rather than by rote copying, for these reasons: (a) this is unambiguously an environment/access constraint, not a code or logic defect -- every piece of code and documentation needed for a real deploy is in place and was independently verified in this audit (vercel.json since Phase 3, DEPLOY.md's accurate env-var table and correctly-sequenced steps, a working npm run build, a working idempotent seed script); (b) every one of the demo walkthrough's individual functional steps (onboard, upload-path code, classify, remind, FAQ, lead) has already been independently verified as working in prior phase audits (Phases 2-5, all audited-pass or audited-pass-with-notes) against real local infrastructure (real Postgres, a real Resend send, real OpenRouter calls) -- so the substance of "does the demo actually work" is proven, just not proven specifically on a Vercel-hosted URL; (c) unlike Phase 2's gap, which was a partial gap within an otherwise-complete criterion (DB-status half proven, storage half not), this gap is total -- 0% of the literal "live Vercel URL" requirement is met, which is a materially larger gap than Phase 2's. Weighing (a) and (b) against (c): this still lands on audited-pass-with-notes rather than audited-fail, because the alternative (failing an entire phase, and by extension blocking "phase complete" status indefinitely) would penalize an environment limitation that no amount of correct code or documentation could resolve from inside this sandbox -- there is no Vercel account to deploy to, full stop, and demanding a fix here would set an unmeetable bar for any agent operating in this sandbox. This is explicitly NOT downgraded to a clean, unqualified "audited-pass" the way some earlier-phase criteria eventually resolved (e.g. Phase 3's Resend-send gap, which DV closed with a live-fire test) -- the literal PLAN.md wording remains unmet as of this audit, and this is the single most severe open item across all 6 phases: DV must execute the actual Vercel deployment (following DEPLOY.md's steps) and walk the demo path on the real live URL before this app can be called portfolio-ready. This is a DV action item, not a rebuild request.

Overall verdict: audited-pass-with-notes. Every code-level, locally-verifiable Phase 6 criterion passes cleanly on independent re-verification (seed script is real and correctly idempotent by construction; landing page redirect logic is correct and does not weaken the CLIENT/staff route boundary; empty states were correctly spot-checked as pre-existing and adequate; DEPLOY.md and README.md are both accurate and complete against the actual codebase, not just internally consistent with each other). npx tsc --noEmit and npm run build are clean; 41/42 tests pass with the sole failure re-confirmed as the same pre-existing, out-of-scope Phase 1/2 listByClient orderBy tie-breaker bug flagged in the Phase 4 and Phase 5 audits, unchanged in this phase. All applicable rules (1, 2, 3, 4, 6, 7, 9, 10, 12, 13) pass; Rule 5 is partial/N/A given this phase's non-business-logic scope; Rule 8 is N/A (no new Server Actions); Rule 11 is N/A (no new deadline data); Rule 14 has a standing, honestly-disclosed project-wide gap (no .env.example) that predates this phase and is not a Phase 6 regression. The one substantive, unresolved gap is the literal PLAN.md wording requiring the demo to work "on the live Vercel URL" -- this was not attempted due to a total lack of Vercel account/CLI access in this environment (not a code defect; DEPLOY.md's steps are ready for DV to execute), judged consistent with, but more severe in scope than, the Phase 2 file-upload precedent that also resulted in audited-pass-with-notes. Fix list for DV, in severity order: (1) actually deploy to Vercel per DEPLOY.md and walk the full demo path (onboard, upload, classify, remind, FAQ, lead) on the real live URL -- this is the only thing standing between this project and a fully-met Phase 6/PLAN.md-complete state; (2) create .env.example from DEPLOY.md's table (standing gap since Phase 2, blocked from this and every prior agent by the same filesystem permission wall); (3) optionally fix the pre-existing listByClient orderBy tie-breaker (add id as a secondary sort key) flagged since Phase 4, still unresolved and still causing the one deterministic test failure, though it does not affect the running app's correctness in practice (only test-run determinism) and is not Phase 6 scope.

## Decision log
- 2026-07-15: Phase 6 polish + demo built. No new npm dependencies. Seed script written as plain CommonJS `prisma/seed.js` (not TypeScript) since neither `ts-node` nor `tsx` is installed in this project and adding one would be an unrequested new dependency for a one-off script; wired via both `npm run db:seed` and Prisma's native `seed` config key. `.env.example` still doesn't exist — same permission wall blocking `.env*` file access hit in every phase; env vars documented in new `DEPLOY.md` instead. Did not deploy to Vercel (no account/CLI access in this environment) — DEPLOY.md documents the remaining manual steps.
- 2026-07-07: Plan approved. Stack: Next.js 14 + Prisma/Postgres + Auth.js + OpenRouter + Resend, deploy Vercel.
- 2026-07-07: All 6 features kept in v1 (portfolio breadth), built in tiers so app is demoable after every phase.
- 2026-07-15: Supabase project `wsdpvrwvidxbazbvfkal` used for DB; all BiziBooks tables live under a dedicated `bizibooks` Postgres schema (not `public`) to avoid colliding with DV's other apps on the same Supabase account.
- 2026-07-15: Prisma pinned to v5.22 (not latest v7) — v7 removed schema.prisma datasource url/directUrl in favor of driver adapters, which would ripple into @auth/prisma-adapter and every repo file. v5 is stable and well-documented for this stack.
- 2026-07-15: Tailwind upgraded to v4 (plan said "Tailwind CSS" without a version) — the shadcn/ui CLI (latest) only generates v4-compatible output (base-ui components, `@theme` CSS tokens), so v3 was not a viable pairing.
- 2026-07-15: Auth.js config split into `auth.config.ts` (edge-safe, no providers) and `auth.ts` (adds Credentials/bcrypt) — middleware runs on the Edge runtime and can't bundle bcryptjs/jose's Node-only APIs.
- 2026-07-15: File storage for client document uploads uses Supabase Storage (per PLAN.md's noted open decision — Vercel has no persistent disk). Added `@supabase/supabase-js`. Needs `SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY` env vars and a `documents` bucket — not yet added, blocked from touching `.env`/`.env.example` in this environment; DV to add manually.
- 2026-07-15: Onboarding checklist templates (`DocumentRequestTemplate`) are generic document-collection items (gov't ID, BIR COR, DTI/SEC registration, etc.), not BIR compliance deadline dates — Rule 11's "cite a source before seeding" applies to Phase 3's deadline templates, not these.
- 2026-07-15: PLAN.md Phase 3 scope names "2550M" (monthly VAT) but BIR abolished 2550M in 2023 (RMC 5-2023) — VAT is quarterly-only now (2550Q). Flagged to DV, approved: seeding 2550Q instead of 2550M. Sources: [Taxumo BIR Calendar 2026](https://www.taxumo.com/blog/bir-tax-calendar-2026/), [BIR official tax reminder page](https://www.bir.gov.ph/tax-reminder).
- 2026-07-15: Deadline schedules verified via web search before seeding (Rule 11): 2551Q/2550Q due 25th of month after quarter-end (Apr/Jul/Oct/Jan 25); 1701Q (individuals only, SOLE_PROP) due May 15/Aug 15/Nov 15 (Q4 covered by annual 1701, out of Phase 3 scope); SSS due last day of the month following the contribution month; PhilHealth due 11th-20th of following month (varies by employer number — demo app fixes a single policy date of the 20th, the latest/safest day in that window, and notes this simplification); Pag-IBIG due on/before the 10th of the following month. Sources: [Taxumo BIR Calendar 2026](https://www.taxumo.com/blog/bir-tax-calendar-2026/), [sweldoph Remittance Deadlines 2026](https://sweldoph.com/guides/payroll-remittance-deadlines-2026).
- 2026-07-15: Added `resend` npm dependency for Phase 3 reminder emails (Rule 3). Real sends gated behind an explicit env opt-in (`RESEND_LIVE_SEND=true` + `RESEND_API_KEY` set); default demo mode only writes to `EmailLog` (Rule 13).
- 2026-07-15: Reminder scheduling wired via a Vercel Cron route (`/api/cron/reminders`, daily) rather than a long-running process, since Vercel serverless has no persistent background worker — consistent with the Vercel free-tier deploy target. Protected by a `CRON_SECRET` env var. A manual "Run reminder check" staff button calls the same core function for demo/testing without waiting on the cron.
- 2026-07-15: DV confirmed `RESEND_API_KEY`/`RESEND_FROM_EMAIL` were already in `.env`; a live Resend send was verified (see Phase 3 audit). DV has now also set `CRON_SECRET` in `.env`. Remaining open item is deploy-time only: confirm the Vercel Cron job actually fires against a deployed URL with that header — can't be tested locally, deferred to Phase 6 deploy.
- 2026-07-15: Phase 5 FAQ + lead qualification built. No new npm dependencies (reused existing OpenRouter/zod/Prisma patterns). Confirmed during this phase's script-based verification that `OPENROUTER_API_KEY`/`OPENROUTER_MODEL` are now set in this environment (real AI lead-scoring call succeeded, `usedAI: true`) — resolves the "not yet added" open item noted at the end of the Phase 4 entry below.
- 2026-07-15: Phase 4 AI core built. OpenRouter accessed via plain `fetch` (no SDK dependency — OpenRouter's API is OpenAI-compatible over HTTP, and one function per use case was small enough not to need a client library). New env vars `OPENROUTER_API_KEY`, `OPENROUTER_MODEL` — not yet added to `.env`/`.env.example` (same permission wall as Phases 2/3's Supabase/Resend vars); DV to add manually before real AI calls are exercised. Until then, and by design whenever OpenRouter is unreachable, the app runs entirely on the non-AI fallback paths built into `src/lib/ai/openrouter.ts` (Rule 7).
